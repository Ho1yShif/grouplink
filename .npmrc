# A single physical copy of @renderinc/sdk is required so the Workflows
# TaskRegistry (a module-level singleton) is shared across every task package.
# Two copies => tasks register against different runtimes and silently fail.
dedupe-peer-dependents=true
auto-install-peers=true
